services:
  inplan-tool:
    build: .
    ports:
      - "5001:5001"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./.cookie_cache.json:/app/.cookie_cache.json
    restart: unless-stopped
